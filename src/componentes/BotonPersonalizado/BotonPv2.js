/* import React from "react";

function BotonPv2(props) {
  const [config, setConfig] = useState({
    bgColor: "",
    textColor: "",
    onClick: () => {},
    className: "",
  });

  switch (props.type) {
    case "danger":
      setConfig({
        ...config,
        bgColor: "red",
        className: "danger",
        textColor: "white",
        onClick: props.onDangerClick,
      });

      break;
    case "success":
      className = "success";
      onClick = props.onSuccessClick;
      break;
    default:
      bgColor = "gray";
      textColor = "black";
      onClick = props.onClick;
  }

  return <div>BotonPv2</div>;
}

export default BotonPv2; */
